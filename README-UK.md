[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT)
[![Rust](https://img.shields.io/badge/Rust-1.70%2B-orange.svg)](https://www.rust-lang.org/)
[![Fedora](https://img.shields.io/badge/Fedora-39%2B-blue.svg)](https://fedoraproject.org/)

> [🇬🇧 English](README.md) | [🇪🇸 Español](README-ES.md) | [🇫🇷 Français](README-FR.md) | [🇩🇪 Deutsch](README-DE.md) | 🇺🇦 Українська | [🇨🇳 中文](README-ZH.md)

# Fedora Utility Patchworker - FUP

**Fedora Utility Patchworker - FUP** — це надійний інструмент на базі Rust, створений для автоматизації оновлень системи, Flatpak та прошивки на Fedora Linux. Він має безпечні налаштування за замовчуванням, повне журналювання та сучасний CLI-інтерфейс.

---

## ✨ Можливості

| Можливість | Опис |
|------------|------|
| **Безпечні Налаштування** | Показує довідку при запуску без прапорців — вимагає явної дії |
| **Попередній Перегляд** | Перевірка доступних оновлень перед встановленням за допомогою `--refresh` |
| **Системні Оновлення** | Автоматичні оновлення пакетів dnf5 з оновленням метаданих |
| **Оновлення Flatpak** | Підтримка всіх застосунків Flatpak в актуальному стані |
| **Оновлення Прошивки** | Опціональна інтеграція fwupd для прошивки UEFI/пристроїв |
| **Розумне Виявлення Перезавантаження** | Запитує перезавантаження лише коли ядро або критичні оновлення цього вимагають |
| **Перевірка Мережі** | Підтверджує з'єднання перед початком оновлень |
| **Коректне Завершення** | Обробляє CTRL+C коректно без пошкодження даних |
| **Повне Журналювання** | Логи з мітками часу в `/var/log/fup.log` |
| **Налаштовуваність** | Підтримка конфігураційного файлу TOML з розумними налаштуваннями |
| **Індикатори Прогресу** | Спінери та вивід в реальному часі |
| **Режим Симуляції** | Попередній перегляд дій без виконання |

---

## 📋 Вимоги

| Вимога | Деталі |
|--------|--------|
| **Операційна Система** | Fedora Linux 39+ (або сумісні дистрибутиви) |
| **Менеджер Пакетів** | `dnf5` для системних оновлень |
| **Опціонально** | `flatpak` для оновлень Flatpak |
| **Опціонально** | `fwupdmgr` для оновлень прошивки |
| **Збірка** | Rust 1.70+ (лише при збірці з вихідного коду) |

---

## 📦 Встановлення

### Попередньо Зібраний Бінарний Файл

1. Завантажте останній бінарний файл зі сторінки [Releases](https://github.com/overdox/fedora-utility-patchworker/releases)
2. Встановіть його:

```bash
sudo mv fup /usr/local/bin/
sudo chmod +x /usr/local/bin/fup
```

### Збірка з Вихідного Коду

```bash
# Клонуйте репозиторій
git clone https://github.com/overdox/SysUpdater.git
cd SysUpdater

# Зберіть з оптимізаціями
cargo build --release

# Встановіть
sudo mv target/release/fup /usr/local/bin/
```

---

## 🚀 Використання

Запуск `fup` без аргументів показує довідку:

```
╔═══════════════════════════════════════════╗
║               FUP v2.1.0                  ║
║     Fedora Utility Patchworker            ║
╚═══════════════════════════════════════════╝

USAGE

    sudo fup [OPTIONS]

COMMANDS

    -r, --refresh         Перевірити та показати доступні оновлення
    -u, --update-all      Оновити все (система + flatpak)
        --update-system   Оновити лише системні пакети (dnf5)
        --update-flatpak  Оновити лише застосунки Flatpak
        --update-firmware Оновити лише прошивку
```

### Швидкі Приклади

```bash
# Показати доступні оновлення
sudo fup --refresh

# Оновити систему та пакети Flatpak
sudo fup --update-all

# Оновити все включаючи прошивку
sudo fup --update-all --firmware

# Оновити лише системні пакети
sudo fup --update-system

# Попередній перегляд що станеться (симуляція)
sudo fup --update-all --dry-run

# Тихий режим для скриптів/cron
sudo fup --update-all --quiet --no-reboot-prompt
```

---

## 📖 Команди

| Команда | Коротко | Опис |
|---------|---------|------|
| `--refresh` | `-r` | Перевірити та показати оновлення без встановлення |
| `--update-all` | `-u` | Оновити системні пакети та застосунки Flatpak |
| `--update-system` | | Оновити лише системні пакети dnf5 |
| `--update-flatpak` | | Оновити лише застосунки Flatpak |
| `--update-firmware` | | Оновити лише прошивку |

---

## ⚙️ Опції

| Опція | Коротко | Опис |
|-------|---------|------|
| `--firmware` | `-f` | Включити оновлення прошивки при `--update-all` |
| `--dry-run` | `-n` | Попередній перегляд дій без виконання |
| `--no-reboot-prompt` | | Пропустити запит на перезавантаження після оновлень |
| `--no-network-check` | | Пропустити перевірку мережевого з'єднання |
| `--parallel` | | Виконувати оновлення паралельно |
| `--config <ФАЙЛ>` | `-c` | Використати власний конфігураційний файл |
| `--verbose` | `-v` | Збільшити деталізацію (`-vv` або `-vvv` для більшої) |
| `--quiet` | `-q` | Мінімальний вивід |

---

## 🔧 Конфігурація

SysUpdater шукає конфігураційні файли в такому порядку:

1. Шлях вказаний через `--config`
2. `/etc/fup.toml`
3. `~/.config/fup/config.toml`

### Приклад Конфігурації

```toml
[system]
enabled = true       # Увімкнути системні оновлення dnf5
auto_remove = true   # Автоматично видаляти невикористовувані пакети
refresh = true       # Оновлювати метадані перед оновленням

[flatpak]
enabled = true       # Увімкнути оновлення Flatpak
remove_unused = true # Видаляти невикористовувані середовища Flatpak

[firmware]
enabled = false      # Оновлення прошивки вимкнено за замовчуванням

[logging]
file = "/var/log/fup.log"
level = "info"       # Варіанти: error, warn, info, debug, trace

[network]
check_url = "https://fedoraproject.org"
timeout_secs = 10
```

---

## 📤 Коди Виходу

| Код | Значення |
|-----|----------|
| `0` | Успіх |
| `1` | Загальна помилка |
| `130` | Скасовано користувачем (CTRL+C) |

---

## 🤝 Внески

Внески вітаються! Ось як ви можете допомогти:

1. **Зробіть форк** репозиторію на GitHub
2. **Створіть** нову гілку для вашої функції або виправлення
3. **Внесіть** зміни та закомітьте їх
4. **Відкрийте** Pull Request в оригінальному репозиторії

Будь ласка, переконайтеся, що ваш код відповідає найкращим практикам Rust та містить відповідні тести.

---

## 📄 Ліцензія

Цей проект ліцензовано за ліцензією MIT — див. файл [LICENSE](LICENSE) для деталей.

---

<p align="center">
  Зроблено з ❤️ для спільноти Fedora
</p>